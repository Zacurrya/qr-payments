server:
  port: 8088
spring:
  datasource:
    url: jdbc:postgresql://aws-0-eu-north-1.pooler.supabase.com:5432/postgres?sslmode=require&prepareThreshold=0
    username: postgres.tuoqiunkzfqytdjohlcs
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  application:
    name: qr-payments

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

supabase:
  url: ${SUPABASE_URL:https://tuoqiunkzfqytdjohlcs.supabase.co}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}