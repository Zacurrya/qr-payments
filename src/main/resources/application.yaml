spring:
  datasource:
    url: jdbc:postgresql://db.tuoqiunkzfqytdjohlcs.supabase.co:5432/postgres?user=postgres&password=Zaky160706!!!sslmode=require
    username: postgres
    password: Zaky160706!!!
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
    # ensures consistency with the db
      ddl-auto: validate

  application:
    name: qr-payments
