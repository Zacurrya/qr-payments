spring:
  datasource:
    url: jdbc:postgresql://db.tuoqiunkzfqytdjohlcs.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
    # ensures consistency with the db
      ddl-auto: validate

  application:
    name: qr-payments
